{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "backend",
            "program": "${workspaceFolder}/cmd/backend/main.go",
            "type": "go",
            "request": "launch",
            "envFile": "${workspaceFolder}/.env",
            "mode": "auto",
            "cwd": "${workspaceFolder}",
        }
    ]
}